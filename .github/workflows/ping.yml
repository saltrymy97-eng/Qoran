name: ping
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit Streamlit app
        run: |
          curl -s -o /dev/null -w "%{http_code}" "https://ldqdgjrfdhzsmbgj3p22gn.streamlit.app/"
